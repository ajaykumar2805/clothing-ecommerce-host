# Backend API server connection URL
VITE_API_URL=http://localhost:5000/api

# Stripe Publishable Token (Test Mode)
# Can contain any valid format or key, falls back to a sandbox simulator if mock mode is on the backend
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_yourPublishableKeyHere
